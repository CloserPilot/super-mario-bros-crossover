const GameSettings = {
    DEBUG_MODE: true, // Let's keep this on for now
    initialLivesCount: { lifeCount: 3 },
    attackStrength: { strength: 1 },
    difficulty: 'Normal',
    FIRST_LEVEL: '1-1',
    DEFAULT_CHARACTER: 0,
    // ... other settings will be added as needed
};

export default GameSettings;
